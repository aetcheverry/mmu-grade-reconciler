name: mmu-grade-reconciler
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - tk                    # ensures Tkinter is included (bundled in Anaconda)
  - pip
  - pip:
      - pandas==2.2.2
      - openpyxl==3.1.3
      - odfpy==1.4.1
